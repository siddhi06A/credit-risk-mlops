data:
  n_samples: 5000
  test_size: 0.2
  random_state: 42

model:
  type: random_forest
  n_estimators: 100
  max_depth: 10

mlflow:
  experiment_name: "credit_risk_experiment"
  tracking_uri: "http://host.docker.internal:5001"
