FROM python:3.9-slim

WORKDIR /workspace

RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

RUN useradd -m -u 1000 developer && chown -R developer:developer /workspace
USER developer

CMD ["/bin/bash"]
